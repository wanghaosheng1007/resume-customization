# Source templates

Copy this folder to the project root as `sources/` on first setup:

```bash
# PowerShell
Copy-Item -Recurse -Force examples\sources sources

# bash
cp -r examples/sources sources
```

Then replace example names and facts with real information. The `sources/` directory is gitignored by default for privacy.
